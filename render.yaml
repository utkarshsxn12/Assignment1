services:
  - type: web
    name: splitsync
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npx prisma db push && node prisma/seed.js && npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
